{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "node",
        "request": "launch",
        "name": "Launch Express",
        "program": "${workspaceFolder}/bin/www",
        "cwd": "${workspaceFolder}",
        "console": "integratedTerminal"
      }
    ]
  }
  